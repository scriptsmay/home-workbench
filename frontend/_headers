# 缓存策略声明（Cloudflare Pages 原生支持此文件；Caddy 侧见服务器 Caddyfile）
#
# 背景：本站静态资源是无内容指纹的裸文件名（app.js / style.css），一旦被浏览器
# 长期缓存，改版后设备会持续跑旧代码——2026-09-21 的数据覆盖事故中，"设备仍在
# 运行修复前的 app.js" 就是助推因素之一。
#
# 因此统一采用协商缓存：允许本地缓存，但每次使用前必须向服务器验证。
# 服务器已提供 ETag / Last-Modified，未变更时返回 304 空响应，带宽开销可忽略；
# 变更后立即生效，不需要用户手动强刷。
#
# 注意：no-cache 不等于 no-store —— 它不禁止缓存，只是禁止"不问就用"。
#
# ⚠️ 分平台生效情况（实证见 docs/ 与知识库 deployment-runbook）：
#   · Cloudflare Pages 原生域名   → 本文件生效
#   · Cloudflare 自定义域名(橙云) → 可能被面板的 Browser Cache TTL 覆盖（默认 4 小时），
#                                  需在 CF 面板将 Browser Cache TTL 设为 "Respect Existing Headers"
#   · 自建 Caddy 主站             → 本文件不生效，策略在服务器 Caddyfile 的 header 指令里
#   · Vercel                      → 本文件不生效，默认已是 max-age=0, must-revalidate
/*
  Cache-Control: no-cache
